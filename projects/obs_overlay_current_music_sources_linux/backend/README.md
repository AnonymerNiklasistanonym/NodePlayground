# obs-overlay-current-music-sources (backend)

A REST API that provides access to the `current_music_sources_linux` binary which supplies a JSON of the current music sources in Linux provided by DBUS.

## Build

**WIP**

```sh
npm install
npm run dev
```

**Requires the built binary `current_music_sources_linux` to exist!**
